#########################################################
# Ingress for Moleculer API Gateway
#########################################################
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  ingressClassName: traefik
  rules:
  - host: api.baoleme.xyz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api
            port:
              number: 3000

# ==============================
# Ingress 路由规则
# ==============================
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: api.baoleme.xyz   # 修改域名
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 3000

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: auth.baoleme.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: auth-mail
                port:
                  number: 3000

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mail-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: mail.baoleme.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: auth-mail
                port:
                  number: 3000

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: minio.baoleme.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio
                port:
                  number: 9001

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cart-history-item-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: services.baoleme.xyz
      http:
        paths:
          - path: /cart
            pathType: Prefix
            backend:
              service:
                name: cart-history-item
                port:
                  number: 3000
          - path: /history
            pathType: Prefix
            backend:
              service:
                name: cart-history-item
                port:
                  number: 3000
          - path: /item
            pathType: Prefix
            backend:
              service:
                name: cart-history-item
                port:
                  number: 3000

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: order-shop-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: business.baoleme.xyz
      http:
        paths:
          - path: /order
            pathType: Prefix
            backend:
              service:
                name: order-shop
                port:
                  number: 3000
          - path: /shop
            pathType: Prefix
            backend:
              service:
                name: order-shop
                port:
                  number: 3000
